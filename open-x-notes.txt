FQDN mars
LDAP 389
ADMIN 4444
cn=Directory Manager
directory base dc=dtc, dc=hsbc,dc=com


Equivalent non-interactive command-line to setup server:
 C:\opt\opendj\setup.bat --cli --backendType "je" --baseDN "dc=dtc, dc=hsbc,dc=com" --ldapPort "389" --adminConnectorPort "4444" --rootUserDN "cn=Directory Manager" --rootUserPassword "******" --no-prompt --noPropertiesFile
 
 
 OpenAM 
 - for authentication
 - enforcing policies such as authentication mechanisms (username/password, certificates)
 
 
 
 session held in access manager && cached in the policy agent
 
 cookies may be avoided when authenticating across third-party systems  
 - for security reasons
 - to avoid the risk of cookies being tampered with
 - a federation protocol like SAML
 - Fedlet - 
 
for scenarios it may not be possible to use a policy agent for varied reasons
	- deploy web server as a reverse proxy
	- install policy agent on the server
	- delegate all authentication && authorisation to reverse proxy
	- ensure the proxy is never by-passed
	
	
domain cookies are ristricted to applications in the same domain

authorisation
- who	
- what actions
- what resources
- under what preconditions

federation- connected identities on different infrastructures

OAUTH 2 
- provides authorisation
- sometimes misused for authentication
- OpenID Connect combines identity && authorisation to   


idm/EndUser